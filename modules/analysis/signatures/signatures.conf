# Signatures analysis configuration
targets = list
flagged_csv = tmp/flagged.csv

# Executable-like extensions to analyze from flagged.csv
extensions = .exe,.dll,.sys,.scr,.com,.ocx,.drv,.efi

# Number of strings lines to include when signature verification fails
strings_limit = 10
